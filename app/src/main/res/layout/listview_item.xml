<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/listViewRealitiveLayout"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/activity_vertical_margin">

    <TextView
        android:id="@+id/contactName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Contact name"
        android:textSize="20dp"
        android:layout_marginLeft="34dp"
        android:layout_marginStart="34dp"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/imageViewContactListView"
        android:layout_toEndOf="@+id/imageViewContactListView"
        android:layout_marginTop="12dp" />

    <TextView
        android:id="@+id/contactNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Number"
        android:textSize="20dp"
        android:layout_marginBottom="13dp"
        android:layout_alignBottom="@+id/imageViewContactListView"
        android:layout_alignLeft="@+id/contactName"
        android:layout_alignStart="@+id/contactName" />

    <ImageView
        android:id="@+id/imageViewContactListView"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/useruser"
        app:srcCompat="@drawable/useruser" />


</RelativeLayout>